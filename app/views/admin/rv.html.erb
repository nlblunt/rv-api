RV Page

<table>
<tr>
<td>Name</td>
<td>Address</td>
<td>Verified</td>
<td>Edit</td>
</tr>

<% @rv.each do |r| %>
    <tr>
        <td><%= r.parkName %></td>
        <td><%= r.address %></td>
        <td><%= r.verified %></td>
        <td><%= link_to 'edit', edit_rvpark_path(r) %></td> 
    </tr>
<% end %>
